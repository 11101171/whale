#!/bin/bash
#############################################
    # port=0000
    # cd ./../
    # sed -i '' "s/^http.port.*/http.port = ${port}/g" ./conf/app.conf
    # sed -i '' "s/^runmode.*/runmode = ${runmode}/g" ./conf/app.conf
    
# cd ./../
# if [ 'whale.tar.gz' -f ];then 
    # rm whale.tar.gz
# fi
# GOOS=linux GOARCH=amd64 revel package whale
# cd ./../../
# application='app-dev-9999'
     # tar -zxvf whale.tar.gz ./whale/deploy/${application}/
     # tar -zxvf whale.tar.gz ./whale/deploy/app-dev-9999/
echo '111' > pid
